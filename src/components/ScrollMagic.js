/*
import React from 'react';
import {Controller, Scene} from 'react-scrollmagic';

const ScrollMagic = () => {
    return (
        <div>
            {/!* Other React components here *!/}
            <Controller>
                <Scene
                    triggerElement="#intro-section"
                    triggerHook={0}
                    duration="200%"
                    pin
                >
                    {(progress, event) => (
                        <div className="intro-image" style={{position: 'fixed', top: '0'}}>
                            {/!* Your content here *!/}
                        </div>
                    )}
                </Scene>
            </Controller>
            {/!* Other React components here *!/}
        </div>
    );
};

export default ScrollMagic;
*/
